// Write a function that takes a number as argument
// If the number is even, return true
// Otherwise, return false

const verifyEven = num => {
    return res = num%2 == 0 ? true : false;
};

console.log(verifyEven(175));
